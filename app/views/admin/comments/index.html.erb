<h3>User comments</h3>
<% @comments.each do |comment| %>
  <%= comment.comment %>
  <%= link_to '削除', admin_user_comment_path(comment.user, comment), method: :delete, data: {confirm: "本当に削除しますか？"} %>
<% end %>