<% if users.present? %>
  <% users.each do |user| %>
    <div class="container border pt-2 my-2">
      <%= link_to user_path(user) do %>
        <%= image_tag user.get_image, class: 'user_image' %>
        <%= user.name %>
      <% end %>
      <div class="user-index-text text-secondary">投稿数：<%= user.posts.count %></div>
      <div class="user-index-text text-secondary"><%= user.created_at.strftime('%Y年%m月') %>よりSnuggleを利用しています。</div>
      <!--<p><%#= user.introduction %></p>-->
    </div>
  <% end %>
<% else %>
  <p>現在、登録ユーザーはいません。</p>
<% end %>